const EOL = require('os').EOL;

module.exports = {
   description: 'Register test helpers',

   afterInstall(options) {
     return this.insertIntoFile(
       'tests/helpers/start-app.js', 
       `${EOL}import './ticketfly-metrics-test-helper';`,
       { after: `import Ember from 'ember';` }
     );
   }
};
